// This file is intentionally left empty as we're using the App Router in Next.js 13+
// The root layout (app/layout.tsx) now handles the HTML document structure

// This file exists to prevent the "<Html> should not be imported outside of pages/_document" error
// by providing a no-op implementation

export default function Document() {
  return null;
}
