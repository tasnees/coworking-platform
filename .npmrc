# Disable running package scripts during installation
ignore-scripts=true

# Use pnpm's node-linker to avoid symlink issues
node-linker=hoisted

# Enable strict-peer-dependencies to catch potential issues
strict-peer-dependencies=false

# Disable the lockfile verification to prevent installation failures
prefer-frozen-lockfile=false
